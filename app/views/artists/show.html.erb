<h2><%= @artist.name %></h2>
<table style="width:500px" border="1px solid black">
  <tr>
    <th>Song</th>
    <th>Genre</th>
  </tr>
  <% @artist.songs.each do |song| %>
    <tr>
      <td><a href="/songs/<%= song.slug %>"><%= song.name %></a></td>
      <% song.genres.each do |genre| %>
        <td><a href="/genres/<%= genre.slug %>"><%= genre.name.capitalize %></a></td>
      <% end %>
    </tr>
  <% end %>
</table>
