<!DOCTYPE html>
<html>
    <head>
        <title>Artists</title>
    </head>
    <body>
    <h1>All Artists</h1>
    <ul>
        <% @artists.each do |artist| %>
            <li><a href="/artists/<%= artist.slug %>"><p><%= artist.name %><p></a></li>
        <% end %>
    </ul>
    </body>
</html>