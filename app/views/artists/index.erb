<h2>Artists</h2>
<% if !@artists.empty? and !@artists.nil? %>
  <ul>
  <% @artists.each do |artist| %>
  <li><a href = "/artists/<%=artist.slug %>"><%= artist.name %></a></li>
  <% end %>
<% end %>
</ul>
