<h2>Here are your SONGS</h2>
<ul>
<% @songs.each { |song| %>
  <li>
    <a href='/songs/<%=song.slug%>'><%=song.name%></a>
     === by <b><%=song.artist.name%></b> === Genres: <%=song.genres.collect{|g| g.name}.join(', ')%>
  </li>
<% } %>
</ul>
