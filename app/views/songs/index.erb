<h1>All Songs!</h1>
<a href="/songs/new">Add New Song</a>

<% @songs.each do |song| %>
  <div class="song">
    <h2>Song: <a href="/songs/<%= song.slug %>"> <%= song.name %></a></h2>
  </div><br>
<% end %>
