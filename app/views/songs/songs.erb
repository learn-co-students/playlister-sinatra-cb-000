<!DOCTYPE html>
<html>
    <head>
        <title>Songs</title>
    </head>
    <body>
    <h1>All Songs</h1>
    <ul>
        <% @songs.each do |song| %>
            <li><a href="/songs/<%= song.slug %>"><p><%= song.name %><p></a></li>
        <% end %>
    </ul>
    </body>
</html>