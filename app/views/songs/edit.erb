<!DOCTYPE html>
<html>
    <head>
        <title>Edit <%= @song.name %></title>
    </head>
    <body>
    <h1>Edit <%= @song.name %></h1>
    <form action="/songs/<%= @song.slug %>" method="post">
    <input id="hidden" type="hidden" name="_method" value="patch">
    <input id="hidden" type="hidden" name="id" value="<%= @song.id %>">

        <label for="song_name">Name:</label>
        <input id="song_name" type="text" name="song[name]" value="<%= @song.name %>">
        <br><br>
        <label for="Artist Name">Artist:</label>
        <input id="Artist Name" type="text" name="artist[name]" value="<%= @song.artist.name %>">
        <br><br>
        <label>Genre</label>
        <br>
        <% Genre.all.each do |genre| %>
            <input id="<%= genre.name %>" type="checkbox" name="genre[][name]" value="<%= genre.name %>" <%= "checked" if @song.genres.include?(genre) %>><%= genre.name %></input>
        <% end %>
        <br><br>
        <input id="submit" type="submit" value="Save">
    </form>
    </body>
</html>