<h1>Artist: <%= @artist.name %></h1>

  <div class="artist">
    <h2>Artist Name: <%= @artist.name %></h2>
    <h4>Artist Songs: <%= @artist.songs.map {|s| "<a href=/songs/"+s.slug+">"+s.name+"</a>"}.to_s %></h4>
    <h4>Artist Genre(s): <%= @artist.genres.map {|g| "<a href=/genres/"+g.slug+">"+g.name+"</a>"}.to_s %></h4>
  </div><br>
